<div class="container center-screen">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
      
       <h2 class="title-center">Mot de passe oublié?</h2>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email,
                placeholder: "jean.bouteille@email.com",
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email", class: "input-style" } %>
  </div>
<div class="container elmt-center">
  <div class="form-actions">
    <%= f.button :submit, "Envoyer les infos", class: "btn-gradient-rose" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
 </div>
</div>
</div>
</div>
